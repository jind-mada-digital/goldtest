Need to take care things about project
---------------------------------

DO NOT UPDATE `offline-mall` plugin
1. we have changes to `offline-mall` plugin in core files so will NEVER update this plugin in future
*In case we need to update it (`offline-mall`) we need to take backup of plugin and compare changes and re add them from old plugin
